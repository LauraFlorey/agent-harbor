# Screenshots

Used on the README. Recapture from a **clean demonstration workspace** (`OMB_DATA_DIR` pointing at an empty directory). Do not commit access codes, API keys, real client chats, or someone else's profile name.

| File | What it shows |
|---|---|
| `hero.png` | First conversation: a bot, the onboarding card, model picker, computer control off |
| `workspace.png` | First run with no AI engine installed — the setup screen a clone-and-run visitor sees |

Older files in this folder (`computer-panel.png`, `marketplace.png`, and similar) are historical OpenMausBot-era captures. Do not feature them on the README until they are recaptured as Agent Harbor with a demo profile.
